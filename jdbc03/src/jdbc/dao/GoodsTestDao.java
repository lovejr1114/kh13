package jdbc.dao;

import org.springframework.jdbc.core.JdbcTemplate;

public class GoodsTestDao {

//	public void insert(GoodsDto dto) {
//		String sql = "insert into goods(no, name, type, price, made) "
//				+ "values (no_seq.nextval, ?, ?, ?, sysdate, sysdate+30)";
//		Object[] data = {dto.getName(), dto.getType(), dto.getPrice(), 
//								dto.getMade(), dto.getExpire()};
//		JdbcTemplate jdbcTemplate = JdbcUtils.getJdbcTemplate();
//		jdbcTemplate.update(sql, data);
//		}
	
//	String sql = "insert into board(board_no, board_title, board_content) "
//				+ "values(board_seq.nextval, ?, ?)";
//	
//	Object[] param = {"boardTitle", "boardContent"};
//	jdbcTemplate.update(sql, param);
}
